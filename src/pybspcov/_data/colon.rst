Colon gene-expression example data
==================================

The resource contains the ``colon`` and ``tissues`` objects distributed with
R ``bspcov`` 1.0.3. Rows returned by ``load_colon`` are samples and columns are
genes; this is the transpose of the upstream 2000-by-62 data frame.
